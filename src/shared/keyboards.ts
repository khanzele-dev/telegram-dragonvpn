import { InlineKeyboard } from "grammy";

export const backToMenuKeyboard = new InlineKeyboard().text("⬅️ Назад", "back_to_menu");

export const backToPlansKeyboard = new InlineKeyboard()
  .text("⬅️ Назад к тарифам", "back_to_plans");

